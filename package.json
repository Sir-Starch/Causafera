{
  "name": "causafera-workspace",
  "version": "0.1.0",
  "private": true,
  "license": "AGPL-3.0-only",
  "packageManager": "pnpm@9.15.9",
  "engines": {
    "node": "^20.0.0 || >=22.0.0",
    "pnpm": "9.15.9"
  },
  "scripts": {
    "build": "pnpm -r build",
    "lint": "pnpm -r lint && pnpm audit:i18n",
    "typecheck": "pnpm -r typecheck",
    "audit:i18n": "node tools/audit/validate-i18n.mjs"
  },
  "devDependencies": {
    "typescript": "^5.6.0"
  }
}
