[package]
name = "causafera-observer"
version.workspace = true
description = "Causafera Observer Desktop Shell"
edition.workspace = true
license.workspace = true

[dependencies]
tauri = { version = "2", features = [] }
causafera-explanation.workspace = true
causafera-lab.workspace = true
causafera-observer-api.workspace = true
causafera-observer-wire.workspace = true
causafera-runtime.workspace = true
causafera-types.workspace = true
thiserror.workspace = true

# Examples only. `hydrology_bench` builds explicit metrics, substrate, and
# forcing schedules, which are geography and domain types; the shipped binary
# links neither.
[dev-dependencies]
causafera-domains.workspace = true
causafera-geography.workspace = true

[build-dependencies]
tauri-build = { version = "2", features = [] }
